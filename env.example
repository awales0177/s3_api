# Data Catalog API Configuration

# Data Source Mode (choose one):
# S3_MODE=true          # Use S3 bucket (recommended for production)
# S3_MODE=false         # Use local _data files (for development)

# S3 Configuration (required when S3_MODE=true)
S3_MODE=true
S3_BUCKET_NAME=dh-api-test
S3_FOLDER_PREFIX=dh-api
AWS_REGION=us-east-2

# AWS Credentials (choose one method):
# Method 1: IRSA (IAM Roles for Service Accounts) - Recommended for Kubernetes
# Leave these empty when using IRSA - the container will automatically assume the IAM role
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Method 2: Explicit credentials (for local development or non-IRSA environments)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# Note: S3_BUCKET_NAME should be just the bucket name, not include a path
# S3_FOLDER_PREFIX is the folder within the bucket where your JSON files are stored
# The API will automatically use the appropriate paths within the bucket

# Cache Configuration
CACHE_DURATION_MINUTES=15

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Authentication
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Logging
LOG_LEVEL=INFO
